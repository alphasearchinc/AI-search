# Grafana admin credentials
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=changeme

# Postgres datasource (Neon in prod, local Postgres in dev)
GF_DB_HOST=postgres:5432
GF_DB_USER=medusa_user
GF_DB_PASSWORD=changeme
GF_DB_NAME=medusa_hovedopgave
GF_DB_SSLMODE=disable
